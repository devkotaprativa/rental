<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand" href="#">Rent</a>
      <%=image_tag("rent.jpeg", :class=>"img-circle", :width=> "50px", :height=> "50px")%> 
    </div>
    <div>
      <ul class="nav navbar-nav">
         <li><span > 
          <%= link_to "Home", user_rooms_path(current_user.id) %> </span></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li><span > 
          <%= link_to "Post Room", new_user_room_path(current_user.id), :class=>"btn btn-primary" %> </span></li> 
      </ul>
    </div>
  </div>
</nav>